const Spinner = () => {
    return (
        <div className="fixed inset-0 flex items-center justify-center bg-[#0A0C0E]">
            <span className="loading loading-spinner loading-xl text-[#D4FF3D]"></span>
        </div>
    );
};

export default Spinner;